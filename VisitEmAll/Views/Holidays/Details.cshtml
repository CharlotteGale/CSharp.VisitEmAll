@section Styles {
    <link rel="stylesheet" href="~/css/details.css" />
}

@using VisitEmAll.ViewModels
@model HolidayDetailsViewModel
@using Microsoft.AspNetCore.Http

@inject IHttpContextAccessor HttpContextAccessor

@{
    var currentUserId = HttpContextAccessor.HttpContext?.Session.GetInt32("User_Id");
}

@if (!string.IsNullOrEmpty(Model.HeroImageUrl))
{
    <div class="hero-image">
        <img src="@Model.HeroImageUrl" alt="Hero Image" />
    </div>
}

<div class="container py-4">

    <h1 class="details-title">@Model.Title</h1>

    @if (!string.IsNullOrEmpty(Model.Location))
    {
        <p class="details-location">@Model.Location</p>
    }

    <!-- ROW 1: SUMMARY + ACTIVITIES -->
    <div class="row g-4 mb-4">

        <!-- Summary -->
        <div class="col-md-6">
            <div class="summary-card card-summary p-4 rounded">

                <h2>Trip Summary</h2>

                @if (!string.IsNullOrEmpty(Model.Location))
                {
                    <p><strong>Location:</strong> @Model.Location</p>
                }

                <p><strong>Plan:</strong> @Model.Days.Count Day Itinerary</p>

                @if (Model.StartDate.HasValue || Model.EndDate.HasValue)
                {
                    <p><strong>Dates:</strong>
                        @Model.StartDate?.ToString("dd MMMM yyyy")
                        @if (Model.StartDate.HasValue && Model.EndDate.HasValue)
                        {
                            @(" - ")
                        }
                        @Model.EndDate?.ToString("dd MMMM yyyy")
                    </p>
                }

@*                 @if (!string.IsNullOrEmpty(Model.DayAccommodation))
                {
                    <p><strong>Accommodation:</strong> @Model.DayAccommodation</p>
                } *@

                @if (Model.TotalCost.HasValue)
                {
                    <p><strong>Total Cost:</strong> £@Model.TotalCost.Value.ToString("0.00")</p>
                }

            </div>
        </div>


        <!-- Activities -->
        <div class="col-md-6">
            <div class="section-card card-activities p-4 rounded">
                <h2>Activities</h2>
                <ul class="activity-list">
                    @foreach (var day in Model.Days)
                    {
                        foreach (var item in day.Items.Where(i => i.ItemType == "Activity"))
                        {
                            <li class="activities-item">
                                <span class="activity-title">@item.Name</span>

                                @if (!string.IsNullOrEmpty(item.Location))
                                {
                                    <span class="location"> - @item.Location</span>
                                }

                                @if (!string.IsNullOrEmpty(item.Notes))
                                {
                                    <div class="notes">@item.Notes</div>
                                }
                            </li>
                        }
                    }
                </ul>

            </div>
        </div>

    </div>


    <!-- ROW 2: ITINERARY DAY 1 + MAP -->
    @if (Model.Days.Count > 0)
    {
        var day1 = Model.Days[0];

        <div class="row g-4 mb-4">

            <!-- Day 1 -->
            <div class="col-md-6">
                <div class="section-card card-itinerary p-4 rounded">
                    <h3>@day1.Date.ToString("dddd, dd MMM yyyy")</h3>

                    @if (!day1.Items.Any())
                    {
                        <p>No items planned for this day.</p>
                    }
                    else
                    {
                        <ul class="timeline">
                            @foreach (var item in day1.Items)
                            {
                                <li class="timeline-item">
                                    <span class="time">@((item.Time?.ToString("HH:mm") ?? "--:--"))</span>
                                    <span class="item-title">@item.Name</span>
                                    <span class="item-type">(@item.ItemType)</span>

                                    @if (!string.IsNullOrEmpty(item.Location))
                                    {
                                        <span class="location"> - @item.Location</span>
                                    }

                                    @if (!string.IsNullOrEmpty(item.Notes))
                                    {
                                        <div class="notes">@item.Notes</div>
                                    }
                                </li>
                            }
                        </ul>
                    }
                </div>
            </div>

            <!-- Map -->
            <div class="col-md-6">
                <div class="map-card p-3 rounded map-placeholder">
                    <div class="">[INSERT MAP HERE]</div>
                </div>
            </div>

        </div>
    }


    <!-- ROW 3: ITINERARY DAY 2 + TRAVEL TIPS -->
    @if (Model.Days.Count > 1)
    {
        var day2 = Model.Days[1];

        <div class="row g-4 mb-4">

            <!-- Day 2 -->
            <div class="col-md-6">
                <div class="section-card card-itinerary p-4 rounded">
                    <h3>@day2.Date.ToString("dddd, dd MMM yyyy")</h3>

                    @if (!day2.Items.Any())
                    {
                        <p>No items planned for this day.</p>
                    }
                    else
                    {
                        <ul class="timeline">
                            @foreach (var item in day2.Items)
                            {
                                <li class="timeline-item">
                                    <span class="time">@((item.Time?.ToString("HH:mm") ?? "--:--"))</span>
                                    <span class="item-title">@item.Name</span>
                                    <span class="item-type">(@item.ItemType)</span>

                                    @if (!string.IsNullOrEmpty(item.Location))
                                    {
                                        <span class="location"> - @item.Location</span>
                                    }

                                    @if (!string.IsNullOrEmpty(item.Notes))
                                    {
                                        <div class="notes">@item.Notes</div>
                                    }
                                </li>
                            }
                        </ul>
                    }
                </div>
            </div>

            <!-- Travel Tips -->
            <div class="col-md-6">
                <div class="section-card card-travel-tips p-4 rounded">
                    <h2>Travel Tips</h2>
                    <ul class="tips-list">
                        <li>Lorem ipsum dolor sit amet consectetur adipiscing elit.</li>
                        <li>Consectetur adipiscing elit quisque.</li>
                        <li>Ex sapien vitae pellentesque sem.</li>
                        <li>Placerat in id cursus mi pretium duis.</li>
                        <li>Pretium tellus duis convallis tempus leo eu aenean.</li>
                    </ul>
                </div>
            </div>

        </div>
    }


    <!-- ROW 4: ITINERARY DAY 3 (FULL WIDTH) -->
    @if (Model.Days.Count > 2)
    {
        var day3 = Model.Days[2];

        <div class="row mb-4">
            <div class="col-12">
                <div class="section-card card-itinerary p-4 rounded">
                    <h3>@day3.Date.ToString("dddd, dd MMM yyyy")</h3>

                    @if (!day3.Items.Any())
                    {
                        <p>No items planned for this day.</p>
                    }
                    else
                    {
                        <ul class="timeline">
                            @foreach (var item in day3.Items)
                            {
                                <li class="timeline-item">
                                    <span class="time">@((item.Time?.ToString("HH:mm") ?? "--:--"))</span>
                                    <span class="item-title">@item.Name</span>
                                    <span class="item-type">(@item.ItemType)</span>

                                    @if (!string.IsNullOrEmpty(item.Location))
                                    {
                                        <span class="location"> - @item.Location</span>
                                    }

                                    @if (!string.IsNullOrEmpty(item.Notes))
                                    {
                                        <div class="notes">@item.Notes</div>
                                    }
                                </li>
                            }
                        </ul>
                    }
                </div>
            </div>
        </div>
    }


    <!-- PHOTOS -->
    <div class="row mb-4">
        <div class="col-12">
            <div class="section-card card-itinerary p-4 rounded">
                <h2>Photos</h2>
                <div class="photo-placeholder">[INSERT PHOTOS HERE]</div>
            </div>
        </div>
    </div>


    <!-- OWNER ACTIONS -->
    @if (currentUserId == Model.OwnerUserId)
    {
        <div class="mt-4">
            <a asp-action="EditHoliday" asp-route-id="@Model.HolidayId" class="btn btn-success me-2">Edit Holiday</a>

            <form asp-action="Delete" asp-route-id="@Model.HolidayId" method="post" class="d-inline">
                @Html.AntiForgeryToken()
                <button type="submit" class="btn btn-danger"
                        onclick="return confirm('Are you sure you want to delete this holiday?');">
                    Remove Holiday
                </button>
            </form>
        </div>
    }

</div>